spring:
  profiles:
    active: local
# Global configuration
---
spring:
  main:
    banner-mode: "off"
  servlet:
    multipart:
      enabled: false
logging:
  level:
    org.springframework.boot.autoconfigure.mongo.embedded.EmbeddedMongo: INFO

---
spring:
  profiles: local
  application: 
    name: local
  main:
    banner-mode: "off"
server:
  address: 127.0.0.1
  port: 8020